[pytest]
markers =
    integration: exercises ipcoal simulation and/or TensorFlow training
    simulation: tests requiring the simulation extra
    hpc: tests requiring the HPC extra
    ml: tests requiring the ML extra
testpaths = tests
